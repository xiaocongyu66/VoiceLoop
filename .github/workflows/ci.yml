name: Build & Analyze

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

env:
  FLUTTER_VERSION: '3.27.4'
  JAVA_VERSION: '17'

jobs:
  analyze:
    name: Dart Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get
        working-directory: tts

      - name: Analyze
        run: dart analyze lib/
        working-directory: tts

      - name: Check format
        run: dart format --set-exit-if-changed lib/
        working-directory: tts

  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    needs: analyze
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get
        working-directory: tts

      - name: Build APK
        run: flutter build apk --release --target-platform android-arm64,android-arm,android-x64
        working-directory: tts

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: voiceloop-android-apk
          path: tts/build/app/outputs/flutter-apk/*.apk
          retention-days: 30

  build-ios:
    name: Build iOS (no codesign)
    runs-on: macos-latest
    needs: analyze
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get
        working-directory: tts

      - name: Build iOS (no codesign)
        run: flutter build ios --release --no-codesign
        working-directory: tts

      - name: Upload iOS build
        uses: actions/upload-artifact@v4
        with:
          name: voiceloop-ios-build
          path: tts/build/ios/iphoneos/Runner.app
          retention-days: 30

  release:
    name: Create Release
    runs-on: ubuntu-latest
    needs: [build-android, build-ios]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: voiceloop-android-apk
          path: artifacts/android

      - uses: actions/download-artifact@v4
        with:
          name: voiceloop-ios-build
          path: artifacts/ios

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            artifacts/android/*.apk
            artifacts/ios/Runner.app
          draft: false
          prerelease: false
